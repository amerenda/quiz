apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: quiz-amer-dev
  namespace: quiz
spec:
  endpoints:
    - dnsName: quiz.amer.dev
      recordTTL: 300
      recordType: A
      targets:
        - 10.100.20.203
