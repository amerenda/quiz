apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: quiz-amer-dev
  namespace: quiz
spec:
  secretName: quiz-amer-dev-tls
  dnsNames:
    - quiz.amer.dev
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
